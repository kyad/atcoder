標準入力全取得して、改行で区切り、全入力に代入
全入力[0]を「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Kに代入
入力行@2を整数変換して、1を引いて、Tに代入
全入力[1]を「 」で区切り、Aに代入
Aを配列数値変換

B=[]
答え=0
iを0からN-1まで繰り返す
  もし、iがTならば、
    答えをA[i]だけ増やす
  違えば、
    BにA[i]を配列プッシュ
  ここまで
ここまで

Bを配列数値ソート
Bを配列逆順

iを0からK-2まで1ずつ増やし繰り返す
  答えをB[i]だけ増やす
ここまで

答えと言う

「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Kに代入
「」と尋ねてSに代入

x=0
📖={}

iを0からN-Kまで繰り返す
  Sのi+1からKを文字抜き出しして部分文字列に代入
  個数=0
  jを0からN-Kまで繰り返す
    Sのj+1からKを文字抜き出しして比較対象に代入
    もし、部分文字列が比較対象と等しいならば、
      個数=個数+1
    ここまで
  ここまで
  もし、個数>xならば、
    x=個数
    📖={}
    📖@部分文字列=1
  違えば、もし、個数==xならば、
    📖@部分文字列=1
  ここまで
ここまで

●(Aを)配列ダンプとは
N=Aの配列要素数
iを0からN-1まで繰り返す
A[i]を継続表示
もし、i<N-1ならば、
「 」を継続表示
ここまで
ここまで
「」を表示
ここまで

xと言う
📖の辞書キー列挙。配列ソート。配列ダンプ

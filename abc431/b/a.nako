「」と尋ねて整数変換してXに代入する。
「」と尋ねて整数変換してNに代入する。
「」と尋ねて「 」で区切り、Wに代入する。
Wを配列数値変換する。
0をNだけ配列要素作成して、Aに代入する。  # 持ってるかフラグ

答え=X
「」と尋ねて整数変換してQに代入する。
Q回
  「」と尋ねて整数変換してPに代入する。
  Pから1を引いてそれに代入する。
  もし、A[それ] == 0ならば、
    A[それ] = 1
    答えにW[それ]を足して答えに代入する。
  違えば、
    A[それ] = 0
    答えからW[それ]を引いて答えに代入する。
  ここまで。
  答えと言う。
ここまで。

「」と尋ねて「 」で区切り、入力行に代入する。
入力行@0を整数変換して、Nに代入する。
入力行@1を整数変換して、Mに代入する。
入力行@2を整数変換して、Kに代入する。

「」と尋ねて「 」で区切り、Hに代入する。
Hを配列数値変換する。

「」と尋ねて「 」で区切り、Bに代入する。
Bを配列数値変換する。

Hを配列数値ソート。
Bを配列数値ソート。

iを0からK-1まで繰り返す。
  もしH[i] > B[M-K+i]ならば、
    「No」と言う。
    終了。
  ここまで。
ここまで。

「Yes」と言う。

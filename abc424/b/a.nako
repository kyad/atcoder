「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Mに代入
入力行@2を整数変換して、Kに代入

0をNだけ配列要素作成してACに代入
iを0からN-1まで繰り返す
    いいえをMだけ配列要素作成してAC[i]に代入
ここまで

答え=[]
K回
    「」と尋ねて「 」で区切り、入力行に代入
    入力行@0を整数変換して、1を引いてAに代入
    入力行@1を整数変換して、1を引いてBに代入
    AC[A][B]=はい
    OK=はい
    jを0からM-1まで繰り返す
        もしAC[A][j]がいいえならば、OKはいいえ
    ここまで
    もしOKならば、答えにA+1を配列プッシュ
ここまで

●(Aを)配列ダンプとは
N=Aの配列要素数
iを0からN-1まで1ずつ増やし繰り返す
  A[i]を継続表示
  もし、i<N-1ならば、
    「 」を継続表示
  ここまで
ここまで
「」を表示
ここまで

答えを配列ダンプ
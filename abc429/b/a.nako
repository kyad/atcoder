# N M
「」と尋ねて「 」で区切り、入力行に代入する。
入力行@0を整数変換して、Nに代入する。
入力行@1を整数変換して、Mに代入する。

# A1 A2 ... AN
「」と尋ねて「 」で区切り、Aに代入する。
Aを配列数値変換する。

S=Aの配列合計

N回
  i=回数-1
  もし、S-A[i] == Mならば、
    「Yes」と表示。
    終了。
  ここまで。
ここまで。
「No」と表示。

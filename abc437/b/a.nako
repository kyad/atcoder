「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Hに代入
入力行@1を整数変換して、Wに代入
入力行@2を整数変換して、Nに代入

0をHだけ配列要素作成してAに代入
yを0からH-1まで繰り返す
  「」と尋ねて「 」で区切り、A[y]に代入
  A[y]を配列数値変換
ここまで

いいえを91だけ配列要素作成して😱に代入

N回
  「」と尋ねて整数変換して、Bに代入
  😱[B]=はい
ここまで

答え=0

yを0からH-1まで繰り返す
  仮=0
  xを0からW-1まで繰り返す
    もし、😱[A[y][x]]がはいならば、仮=仮+1
  ここまで
  答え=答えと仮の最大値
ここまで

答えと言う

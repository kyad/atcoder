「」と尋ねて整数変換してNに代入
「」と尋ねて「 」で区切り、🔒に代入
🔒を配列数値変換

いいえをN+1だけ配列要素作成して到達可能に代入
到達可能[0]=はい
到達可能[N]=はい

iを0からN-1まで繰り返す
    もし🔒[i]が0ならば、
        到達可能[i+1]=はい
    違えば、
        抜ける
    ここまで
ここまで

iをN-1から0まで繰り返す
    もし🔒[i]が0ならば、
        到達可能[i]=はい
    違えば、
        抜ける
    ここまで
ここまで

答え=0
iを0からNまで繰り返す
    もし到達可能[i]がいいえならば、答えを1増やす
ここまで

答えを言う

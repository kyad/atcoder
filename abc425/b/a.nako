「」と尋ねて整数変換してNに代入する。
「」と尋ねて「 」で区切り、Aに代入する。
Aを配列数値変換する。

●(Aを)配列ダンプとは
N=Aの配列要素数
iを0からN-1まで繰り返す。
  A[i]を継続表示。
  もし、i<N-1ならば、
    「 」を継続表示。
  ここまで。
ここまで。
「」を表示。
ここまで。

dict={}
N回
dict[回数]=1
ここまで

-1をNだけ配列要素作成してPに代入

iを0からN-1まで繰り返す
もし、A@i≠-1 ならば
もし、dictのA@iが辞書キー存在するならば、
dictからA@iを辞書キー削除
P@i=A@i
違えば、
「No」と言う
終了
ここまで
ここまで
ここまで

dictの辞書キー列挙して残りに代入

iを0からN-1まで繰り返す
もし、A@iが-1ならば、
残りから配列ポップしてP@iに代入
ここまで
ここまで

「Yes」と言う
Pを配列ダンプ

「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Kに代入
「」と尋ねて「 」で区切り、Aに代入
Aを配列数値変換

S=[0]
iを1からNまで繰り返す
  S[i]=A[i-1]+S[i-1]
ここまで

答え=0
lを1からN-K+1まで繰り返す
  r=l+K-1
  sum=S[r]-S[l-1]
  もし、sum<=0ならば、答えを1増やす
ここまで

答えと言う

標準入力全取得して、改行で区切り、全入力に代入
全入力[0]を「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Mに代入
全入力[1]をSに代入
全入力[2]をTに代入

●(iへ)Fとは
答え=0
jを0からM-1まで繰り返す
  a=ASC(S[i+j])-ASC("0")
  b=ASC(T[j])-ASC("0")
  もし、a>=bならば、
    答えに(a-b)を足して答えに代入
  違えば、
    答えに(10+a-b)を足して答えに代入
  ここまで
ここまで
答えを戻す
ここまで

無限=10^9

答え=無限
iを0からN-Mまで繰り返す
  now=iへF
  答えとnowの最小値を答えに代入
ここまで

答えと言う

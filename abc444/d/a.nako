「」と尋ねて整数変換して、Nに代入
「」と尋ねて「 」で区切り、Aに代入
Aを配列数値変換

M=2*10^5
c=0を(M+10)だけ配列要素作成

iを0からN-1まで繰り返す
  c[0]を1だけ増やす
  c[A[i]]を1だけ減らす
ここまで

iを1からM+10-1まで繰り返す
  c[i]をc[i-1]だけ増やす
ここまで

// 繰り上げの処理
d=0を(M+10)だけ配列要素作成  // 確定
iを0からM+10-1まで繰り返す
  d[i]=c[i]%10
  // 繰り上げ
  k=1
  c[i]>0の間
    c[i]=INT(c[i]/10)
    c[i+k]を(c[i]%10)だけ増やす
    kを1だけ増やす
  ここまで
ここまで

最初ゼロ=はい
iをM+10-1から0まで1ずつ減らし繰り返す
  もし、最初ゼロ==はい かつ d[i]==0ならば、続ける
  最初ゼロ=いいえ
  d[i]を継続表示
ここまで
「」を表示

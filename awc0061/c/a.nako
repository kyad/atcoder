「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Kに代入
「」と尋ねて「 」で区切り、Aに代入
Aを配列数値変換
Aの0に0を配列挿入
dp=0をN+1だけ配列要素作成
mx=0をN+1だけ配列要素作成
iを1からK+1まで繰り返す
  dp[i]=A[i]
  mx[i]=mx[i-1]とdp[i]の最大値
ここまで
iをK+2からNまで1ずつ増やし繰り返す
  dp[i]=mx[i-K-1]+A[i]
  mx[i]=mx[i-1]とdp[i]の最大値
ここまで
答えはdpの配列最大値
答えと言う

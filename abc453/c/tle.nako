「」と尋ねて整数変換してNに代入
「」と尋ねて「 」で区切り、Lに代入
Lを配列数値変換

N2=SHIFT_L(1,N)
答え=-1
bitを0からN2-1まで繰り返す
  now=0
  x=0.5
  iを0からN-1まで繰り返す
    もし、AND(SHIFT_R(bit,i),1)が1ならば、
      sgn=1
    違えば、
      sgn=-1
    ここまで
    nx=x+sgn*L[i]
    もし、x*nx<0ならば、nowを1増やす
    x=nx
  ここまで
  答え=答えとnowの最大値
ここまで

答えと言う

「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Tに代入
入力行@1を整数変換して、Xに代入
「」と尋ねて「 」で区切り、Aに代入
Aを配列数値変換
前=A[0]
答え=[[0,A[0]]]
tを1からTまで繰り返す
  もし、ABS(前-A[t])>=Xならば、
    答えに[t,A[t]]を配列プッシュ
    前=A[t]
  ここまで
ここまで
答えを反復
  対象[0]&" "&対象[1]と言う
ここまで

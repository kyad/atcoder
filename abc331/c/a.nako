標準入力全取得して、改行で区切り、全入力に代入してください。
全入力[0]を整数変換して、Nに代入してください。
全入力[1]を「 」で区切り、Aに代入してください。
Aを配列数値変換してください。

Aの配列最大値を、Mに代入してください。

0をM+1だけ配列要素作成して、個数配列に代入してください。
iを0からMまで繰り返す。
  個数配列[A[i]]を1増やしてください。
ここまで。

# 大要素和配列[a]は、aより大きな要素の和です。
0をM+1だけ配列要素作成して、大要素和配列に代入してください。
aをM-1から0まで繰り返す。
  大要素和配列[a]は、大要素和配列[a+1] + (a+1) * 個数配列[a+1]です。
ここまで。

# 答えを出力します。
iを0からN-1まで繰り返す。
  大要素和配列[A[i]]を継続表示してください。
  もし、iがN-1ならば、
    「」を表示してください。お願いします。
  違えば、
    「 」を継続表示してください。
  ここまで。
ここまで。

int@N,@K,@(L[N],R[N]);
sortA(N,R,L);
int r=int_inf,l=0;
while(r-l>1){
  int x=(l+r)/2;
  int count=0;
  int right=-int_inf;
  rep(i,N)if(right+x<=L[i])right=R[i],count++;
  if(count>=K)l=x;else r=x;
}
wt(l==0?-1:l);

「」と尋ねて整数変換してNに代入
「」と尋ねて「 」で区切り、Aに代入
Aを配列数値変換

辞書={}

iを0からN-1まで繰り返す
  x=A[i]
  もし、辞書にx-1が辞書キー存在するならば
    y=辞書[x-1]+1
  違えば、
    y=1
  ここまで
  もし、辞書にxが辞書キー存在するならば
    辞書[x]は辞書[x]とyの最大値
  違えば、
    辞書[x]はy
  ここまで
ここまで

答え=0
辞書を反復
  答え=答えと対象の最大値
ここまで

答えと言う

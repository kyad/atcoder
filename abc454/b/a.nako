「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Mに代入
「」と尋ねて「 」で区切り、Fに代入
Fを配列数値変換

C=0をMだけ配列要素作成
iを0からN-1まで繰り返す
  C[F[i]-1]を1だけ増やす
ここまで

もし、(Cの配列最大値)が1ならば、
  "Yes"と言う
違えば、
  "No"と言う
ここまで

もし、(Cの配列最小値)>=1ならば、
  "Yes"と言う
違えば、
  "No"と言う
ここまで

標準入力全取得して、改行で区切り、全入力に代入
全入力[0]を「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Qに代入
全入力[1]を「 」で区切り、Vに代入
Vを配列数値変換
全入力[2]を「 」で区切り、Pに代入
Pを配列数値変換

graph=[]
uを0からN-1まで1ずつ増やし繰り返す
  graph[u]=[]
ここまで
uを1からN-1まで1ずつ増やし繰り返す
  graph[P[u-1]-1]にuを配列プッシュ
ここまで

dp=[]
●(uとparentを)DFSとは
  dp[u]=V[u]
  もし、parentが-1でなければ、dp[u]をdp[parent]だけ増やす
  graph[u]を反復
    DFS(対象,u)
  ここまで
ここまで

DFS(0,-1)

答え=「」
qiを0からQ-1まで繰り返す
  全入力[3+qi]を整数変換して、1を引いてXに代入
  答え=答え&dp[X]
  もし、qiがQ-1でなければ、答え=答え&改行
ここまで

答えと言う

# N
「」と尋ねて整数変換してNに代入する。

# A1 A2 ... AN
「」と尋ねて「 」で区切り、Aに代入する。
Aを配列数値変換する。

答えは0
lを0からN-1まで繰り返す
  rをlからN-1まで繰り返す
    和は0
    iをlからrまで繰り返す
      和は和+A[i]
    ここまで
    OK=はい
    iをlからrまで繰り返す
      それは、和をA[i]で割った余り
      もし、それが0ならば
        OK=いいえ
      ここまで
    ここまで
    もし、OKがはいならば
      答えは答え+1
    ここまで
  ここまで
ここまで

答えと言う

「」と尋ねて「 」で区切り、入力行に代入する。
入力行@0を整数変換して、Xに代入する。
入力行@1を整数変換して、Yに代入する。
入力行@2を整数変換して、Zに代入する。

永遠の間
  もし、X == Y * Zならば、
    答え="Yes"
    抜ける
  違えば、もし、X < Y * Zならば、
    答え="No"
    抜ける
  ここまで
  Xに1を足してXに代入する
  Yに1を足してYに代入する
ここまで

答えと言う

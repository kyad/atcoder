# N
「」と尋ねて整数変換してNに代入する。

# A1 A2 ... AN
「」と尋ねて「 」で区切り、Aに代入する。
Aを配列数値変換する。

iを0からN-1まで繰り返す
  答え=-1
  # jをi-1から0まで1ずつ減らし繰り返す
  jjを0からi-1まで繰り返す
    j = i - 1 - jj
    もし、A[j] > A[i]ならば、
      答え=j+1
      抜ける
    ここまで
  ここまで
  答えと言う
ここまで

「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Kに代入
入力行@2を整数変換して、Mに代入
「」と尋ねて「 」で区切り、Aに代入
Aを配列数値変換

答え=0
sum=0
r=0
lを0からN-1まで1ずつ増やし繰り返す
  // 条件を満たす最大のrを求める
  永遠の間
    もし、sum>=Mかつ(r-l>=K)ならば、抜ける
    nr=r+1
    もし、nr>=N+1ならば、抜ける
    r=nr
    sumをA[r-1]だけ増やす
  ここまで

  もし、sum>=Mかつ(r-l>=K)ならば、答えを(N-r+1)だけ増やす

  // lをインクリメントする前の処理
  もし、l==rならば、
    // このとき、[l, r)は空集合なので、条件からlを除外する処理は不要
    rを1だけ増やす  // 先にrをインクリメントする
  違えば、
    // 条件からlを除外する
    sumをA[l]だけ減らす
  ここまで
ここまで

答えと言う

「」と尋ねて「 」で区切り、入力行に代入
入力行[0]を整数変換して、Nに代入
入力行[1]を整数変換して、Mに代入

グラフ=[]
iを0からN-1まで1ずつ増やし繰り返す
  グラフ[i] = []
ここまで

M回
  「」と尋ねて「 」で区切り、入力行に代入
  入力行[0]を整数変換して、1を引き、Uに代入
  入力行[1]を整数変換して、1を引き、Vに代入
  グラフ[U]にVを配列追加
  グラフ[V]にUを配列追加
ここまで

●(Aを)Fとは
答え=A*(A-1)*(A-2)/(3*2)
答えを戻す
ここまで

答え=0をNだけ配列要素作成
iを0からN-1まで繰り返す
  n=N-1-(グラフ[i]の配列要素数)
  もし、n>=3ならば、答え[i]=F(n)
ここまで

●(Aを)配列ダンプとは
N=Aの配列要素数
iを0からN-1まで1ずつ増やし繰り返す
A[i]を継続表示
もし、i<N-1ならば、
「 」を継続表示
ここまで
ここまで
「」を表示
ここまで

答えを配列ダンプ

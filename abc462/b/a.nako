標準入力全取得して、改行で区切り、全入力に代入する。
全入力[0]を整数変換して、Nに代入する。
答え=[]をNだけ配列要素作成
iを0からN-1まで繰り返す
  全入力[i+1]を「 」で区切り、入力行に代入する。
  K=入力行[0]
  kを1からKまで繰り返す
    答え[入力行[k]-1]にi+1を配列プッシュ
  ここまで
ここまで
iを0からN-1まで繰り返す
  X=答え[i]の配列要素数
  Xを継続表示
  jを0からX-1まで1ずつ増やし繰り返す
    「 」を継続表示
    答え[i][j]を継続表示
  ここまで
「」を表示
ここまで

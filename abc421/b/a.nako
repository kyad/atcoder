「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Xに代入
入力行@1を整数変換して、Yに代入

●(Xを)Fとは
  Xを文字列変換
  文字列分解
  配列逆順
  rev=0
  反復
    rev=rev*10
    revを(ASC(それ)-ASC("0"))だけ増やす
  ここまで
  revを戻す
ここまで

A=[]
A[1]=X
A[2]=Y
iを3から10まで繰り返す
  A[i]=F(A[i-1]+A[i-2])
ここまで
A[10]と言う

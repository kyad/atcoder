全回答=「」
「」と尋ねて整数変換してTに代入
T回
  「」と尋ねて「 」で区切り、入力行に代入
  入力行@0を整数変換して、NAに代入
  入力行@1を整数変換して、NBに代入
  入力行@2を整数変換して、NCに代入
  S=NA+NB+NC
  左=0  // OK
  右=10^9+1  // NG
  右-左>1の間
    mid=INT((左+右)/2)
    もし(NA>=mid)かつ(NC>=mid)かつ(S>=mid*3)ならば、
      左=mid  // OK
    違えば、
      右=mid  // NG
    ここまで
  ここまで
  全回答=全回答&「{左}{改行}」
ここまで
全回答と表示

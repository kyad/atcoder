「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Hに代入
入力行@1を整数変換して、Wに代入
S=[]
yを0からH-1まで繰り返す
  「」と尋ねてS[y]に代入
ここまで
答え="Yes"
dy=[0, 1, 0, -1]
dx=[1, 0, -1, 0]
yを0からH-1まで繰り返す
  xを0からW-1まで繰り返す
    もし、S[y][x]が"#"ならば、
      個数=0
      kを0から3まで繰り返す
        ny=y+dy[k]
        nx=x+dx[k]
        もし、nx<0またはnx>=Wまたはny<0またはny>=Hならば、続ける
        もし、S[ny][nx]が"#"ならば、個数を1だけ増やす
      ここまで
      もし、個数!=2かつ個数!=4ならば、答え="No"
    ここまで
  ここまで
ここまで
答えと言う

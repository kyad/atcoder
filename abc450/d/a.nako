「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Kに代入
「」と尋ねて「 」で区切り、Aに代入
Aを配列数値変換

辞書={}
iを0からN-1まで繰り返す
  辞書[A[i]%K]=1
ここまで
B=辞書の辞書キー列挙
Bを配列数値ソート
M=Bの要素数

答え=0
もし、M>=2ならば、
  mx=B[0]+K-B[M-1]
  iを1からM-1まで繰り返す
    mxはmxとB[i]-B[i-1]の最大値
  ここまで
  答え=K-mx
ここまで

答えと言う

「」と尋ねて整数変換して、Nに代入
答え=0
// 尺取り法。[l, r)は半開区間とする
r=0
lを0からN-1まで1ずつ増やし繰り返す
  // 条件を満たす最大のrを求める
  永遠の間
    nr=r+1
    もし、nr>=N+1ならば、抜ける
    もし、l==nr-1ならば、
      r=nr
      続ける
    ここまで
    // nrが条件を満たしているかチェック
    「? {l+1} {nr-1+1}」と言う
    「」と尋ねて応答に代入
    もし、応答が"No"ならば、抜ける
    r=nr
  ここまで

  // この時点で[l, r)は条件を見たす最大の区間
  答えを(0とr-l-1の最大値)だけ増やす

  // lをインクリメントする前の処理
  もし、l==rならば、
    // このとき、[l, r)は空集合なので、条件からlを除外する処理は不要
    rを1だけ増やす  // 先にrをインクリメントする
  ここまで
ここまで
答えと言う
終了

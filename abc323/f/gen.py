import random

a = -3
b = 3
XA = random.randint(a, b)  # a<=N<=b のNを返す
YA = random.randint(a, b)  # a<=N<=b のNを返す
XB = random.randint(a, b)  # a<=N<=b のNを返す
YB = random.randint(a, b)  # a<=N<=b のNを返す
XC = random.randint(a, b)  # a<=N<=b のNを返す
YC = random.randint(a, b)  # a<=N<=b のNを返す
print(XA, YA, XB, YB, XC, YC)

「」と尋ねて文字列変換してXに代入する。
NはXの文字数
0をNだけ配列要素作成してAに代入する。

iを0からN-1まで繰り返す。
  Xのi+1から1文字抜き出し。
  A[i]はそれ+0
ここまで。

Aを配列数値ソート

状態=0  # 1なら1桁目が確定
答え=[]
B=[]
iを0からN-1まで繰り返す。
  もし、状態が0ならば、
    もし、A[i]が0ならば、
      BにA[i]を配列プッシュ
    違えば、
      答えにA[i]を配列プッシュ
      Bを反復
        答えにそれを配列プッシュ
      ここまで。
      状態は1
    ここまで。
  違えば
    答えにA[i]を配列プッシュ
  ここまで。
ここまで。

iを0からN-1まで繰り返す。
  もしi<N-1ならば、
    答え[i]を継続表示。
  違えば、
    答え[i]を表示。
  ここまで。
ここまで。

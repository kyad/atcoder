「」と尋ねて「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Mに代入
入力行@2を整数変換して、Dに代入
「」と尋ねて「 」で区切り、Tに代入
Tを配列数値変換

●(aをbで)floordivとは
# floor(a / b)を計算する。aが負の場合は負の無限大へ丸める。
もし、a>=0ならば、
a/bを整数変換して答えに代入する
違えば、
-aをbでceildiv
答えはそれ*-1
ここまで。
答えを戻す。
ここまで。

●(aをbで)ceildivとは
# ceil(a / b)を計算する。aが負の場合は正の無限大へ丸める。
もし、a>=0ならば、
(a+b-1)/bを整数変換して答えに代入する
違えば、
-aをbでfloordiv
答えはそれ*-1
ここまで。
答えを戻す。
ここまで。

答え=0
iを0からN-1まで繰り返す
  もし、T[i]>Mならば、答えをceildiv(T[i]-M,D)だけ増やす
ここまで
答えと言う

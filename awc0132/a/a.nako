標準入力全取得して、改行で区切り、全入力に代入
全入力[0]を「 」で区切り、入力行に代入
入力行@0を整数変換して、Nに代入
入力行@1を整数変換して、Pに代入
全入力[1]を「 」で区切り、Hに代入
Hを配列数値変換
答え=0
iを0からN-1まで繰り返す
  もし、P>=H[i]ならば、
    PをH[i]だけ減らす
    答えを1だけ増やす
  違えば、
    PをH[i]だけ増やす
  ここまで
ここまで
答えと言う

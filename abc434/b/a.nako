# N M
「」と尋ねて「 」で区切り、入力行に代入する。
入力行@0を整数変換して、Nに代入する。
入力行@1を整数変換して、Mに代入する。

0をMだけ配列要素作成してSに代入する。# 重さの和
0をMだけ配列要素作成してCに代入する。# 数

iを0からN-1まで繰り返す
  「」と尋ねて「 」で区切り、入力行に代入する。
  入力行@0を整数変換して、Aに代入する。
  入力行@1を整数変換して、Bに代入する。
  A=A-1
  S[A]=S[A]+B
  C[A]=C[A]+1
ここまで

jを0からM-1まで繰り返す
  答え=S[j]/C[j]
  答えと言う
ここまで

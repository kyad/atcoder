「」と尋ねて「 」で区切り、入力行に代入する。
入力行@0を整数変換して、Wに代入する。
入力行@1を整数変換して、Bに代入する。

●(aをbで)floordivとは
# floor(a / b)を計算する。aが負の場合は負の無限大へ丸める。
もし、a>=0ならば、
  a/bを整数変換して答えに代入する
違えば、
  -aをbでceildiv
  答えはそれ*-1
ここまで。
答えを戻す。
ここまで。

●(aをbで)ceildivとは
# ceil(a / b)を計算する。aが負の場合は正の無限大へ丸める。
もし、a>=0ならば、
  (a+b-1)/bを整数変換して答えに代入する
違えば、
  -aをbでfloordiv
  答えはそれ*-1
ここまで。
答えを戻す。
ここまで。

W*1000をBでceildivして答えに代入する
それ=W*1000をBで割った余り
もし、それが0ならば、
  答えに1を足して答えに代入する
ここまで。

答えと言う

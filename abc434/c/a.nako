「」と尋ねて整数変換してTに代入する。
T回
  「」と尋ねて「 」で区切り、入力行に代入する。
  入力行@0を整数変換して、Nに代入する。
  入力行@1を整数変換して、Hに代入する。
  最高高度=H
  最低高度=H
  前時刻=0
  答え="Yes"
  N回
    「」と尋ねて「 」で区切り、入力行に代入する。
    入力行@0を整数変換して、tに代入する。
    入力行@1を整数変換して、lに代入する。
    入力行@2を整数変換して、uに代入する。
    最高高度は、最高高度+(t-前時刻)とuの最小値
    最低高度は、最低高度-(t-前時刻)とlと0の最大値
    もし、最高高度<最低高度なら、
      答え="No"
    ここまで。
    前時刻=t
  ここまで。
  答えと言う
ここまで。

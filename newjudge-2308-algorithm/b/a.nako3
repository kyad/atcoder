●(XにIで)壊れた四捨五入とは
  Aは0
  Bは0
  dは1
  YはX
  kを1からI+1まで繰り返す
    Y/10の整数部分をQに代入する。
    Yを10で割った余りをRに代入する。
    A = A * 10
    A = A + Q
    B = B + (d * R)
    d = d * 10
    Y / 10の整数部分をYに代入する。
  ここまで
  もし、R >= 5ならば
    答え = X-B+d
  違えば
    答え = X-B
  ここまで
  答えを戻す。
ここまで

「」と尋ねて「 」で区切る。
Xはそれ[0]
Kはそれ[1]
Xを整数変換してXに代入する
Kを整数変換してKに代入する
Iを0からK-1まで繰り返す
  XにIで壊れた四捨五入してXに代入する。
ここまで
Xを表示

# https://atcoder.jp/contests/abc443/editorial/15179

標準入力全取得して、改行で区切り、全入力に代入
T=全入力[0]を整数変換

tを0からT-1まで繰り返す
  入力行=全入力[1+t*2]を「 」で区切る
  N=入力行を整数変換
  R=全入力[1+t*2+1]を「 」で区切る
  Rを配列数値変換
  S=Rを配列複製

  xを1からN-1まで繰り返す
    R[x]は(R[x-1]+1)とR[x]の最小値
  ここまで

  #xをN-2から0まで1ずつ減らし繰り返す
  xxを0からN-2まで1ずつ増やし繰り返す
    x=N-2-xx
    R[x]は(R[x+1]+1)とR[x]の最小値
  ここまで

  答え=0
  xを0からN-1まで繰り返す
    答えを(S[x]-R[x])だけ増やす
  ここまで
  答えと言う
ここまで

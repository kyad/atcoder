「」と尋ねて整数変換してNに代入する。

-1をNだけ配列要素作成して盤面に代入
yを0からN-1まで繰り返す
  -1をNだけ配列要素作成して盤面[y]に代入
ここまで

0をyに代入
N/2を整数変換してxに代入

盤面[y][x]=1

kを2からN*Nまで繰り返す
  ny=(y-1+N)%N
  nx=(x+1)%N
  もし、盤面[ny][nx]が-1でなければ、
    ny=(y+1)%N
    nx=x
  ここまで
  盤面[ny][nx]=k
  y=ny
  x=nx
ここまで

●(Aを)配列ダンプとは
N=Aの配列要素数
iを0からN-1まで繰り返す。
  A[i]を継続表示。
  もし、i<N-1ならば、
    「 」を継続表示。
  ここまで。
ここまで。
「」を表示。
ここまで。

yを0からN-1まで繰り返す
  盤面[y]を配列ダンプ
ここまで
